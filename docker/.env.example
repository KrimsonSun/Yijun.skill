# Discord bot token from https://discord.com/developers/applications
# Required.
DISCORD_TOKEN=

# Optional: Discord webhook URL that gets a ping when a user activates
# intimate mode (so you can see if someone unexpectedly unlocked it).
ALERT_WEBHOOK_URL=
